> **ASTRO ADAPTATION:** Site is Astro/Git (not WordPress). Map `wp_*`/RankMath → `new-post.mjs` + frontmatter (`references/seo-fields.md`); "publish" = open/merge a PR (`/approve`); GSC/ahrefs research still applies; performance via Google Analytics + GSC MCPs; social via the blotato MCP when connected. Read `references/*` first.

# Routine 6 — Quarterly Refresh Sweep + URL Database Rebuild

**Schedule:** 1st of January, April, July, October — 09:00 SGT
**Connectors:** arahkaii, ahrefs, gmail
**Repo:** arahkaii-editorial
**Risk level:** None on site (read-only). Writes only to repo files.

---

## Prompt to paste into Routines UI

```
You are the arahkaii.com quarterly maintenance routine. Rebuild the live URL database, identify refresh candidates, queue them in the content calendar.

This routine does NOT modify any WordPress content. Read-only on the site. Writes only to repo files.

PART A — URL DATABASE REBUILD

STEP 0 — CONNECTIVITY
Call arahkaii:mcp_ping. If fails, log and email. Exit.

STEP 1 — PULL ALL PUBLISHED POSTS
Call arahkaii:wp_get_posts:
- status: "publish"
- per_page: 100
- orderby: "modified"
- order: "desc"

Paginate if more than 100. Capture all posts.

STEP 2 — ENRICH EACH POST
For each post (in parallel where possible):
- Call arahkaii:wp_get_post_snapshot:
  - ID: <post_id>
  - include: ["meta", "terms"]
- Capture: ID, permalink, post_name, post_title, primary category (from terms), all tags, post_modified, post_date, word_count (from post_content character count / 5 estimate), rank_math_focus_keyword

STEP 3 — REBUILD references/url-database.md
Replace the entire contents of references/url-database.md with a fresh build:

# URL Database — arahkaii.com
*Live inventory. Auto-generated by Routine 6 on <ISO timestamp>.*

## Categories

### Fashion
[For each post in this pillar, sorted by modified date desc:]
#### <Post Title>
- URL: <permalink>
- Slug: <post_name>
- Tags: <comma-separated tag list>
- Modified: <YYYY-MM-DD>
- Word count: <int>
- Primary keyword: <focus_keyword>
- Pillar: fashion

### Beauty
[Same pattern]

### Culture
[Same pattern]

### Travel
[Same pattern]

### Lifestyle
[Same pattern]

### Sustainability
[Same pattern]

## Topic clusters
[Auto-generate clusters by detecting tag co-occurrence. E.g., posts tagged with at least 3 of [songzio, wooyoungmi, korean-heritage-brands, k-fashion] form the "Korean fashion cluster". Include 8-12 clusters.]

STEP 4 — UPDATE references/category-tag-map.md
Replace its contents with the live category and tag IDs:

# Category & Tag Map — arahkaii.com
*Auto-generated by Routine 6 on <ISO timestamp>*

## Categories
[Table: ID, slug, name, post count]

## Tags
[Table: ID, slug, name, post count — sorted by post count desc]

## Casing duplicates flagged
[List any tags that exist as both "Tag-Name" and "tag-name" — these need manual reconciliation by Robert.]

PART B — REFRESH CANDIDATES

STEP 5 — IDENTIFY DECAY
Filter posts where post_modified is >180 days ago.

For each, call ahrefs:site-explorer-organic-keywords for the URL with date_compared = 90 days ago. Capture:
- Current monthly traffic estimate
- Traffic 90 days ago
- Traffic delta (absolute and %)
- Top ranking keyword and its position trend

STEP 6 — SCORE REFRESH CANDIDATES
Compute refresh score for each post:
  score = (days_since_modified / 30) + (traffic_drop_% / 10) + (10 if top keyword position > 10 else 0)

Higher score = higher refresh priority.

Top 15 = quarterly refresh queue.

STEP 7 — APPEND REFRESH CANDIDATES TO CALENDAR
For each of the top 15, append to content-calendar.md under a new "## Refresh Queue Q<N>YYYY" section:

### YYYY-MM-DD | status:refresh | pillar:<pillar> | words:<original_words+20%>
**Title:** [Refresh] <original title>
**Original published:** <original date>
**Last modified:** <last modified date>
**Current keyword position:** <pos> for "<keyword>"
**Traffic trend:** <delta>% over 90 days
**Refresh angle suggestion:** [Auto-suggest based on the post's pillar and current performance — see suggestions below]
**Target keywords:** <existing focus keyword + 2-3 related from ahrefs>

Refresh angle suggestions (auto-generate based on pillar + decay pattern):
- "Update stats and references to current year/season" (default)
- "Expand thin sections; check word count vs current top ranking competitors"
- "Add FAQ section for AI Overview eligibility"
- "Refresh featured image; add visual hierarchy"
- "Add internal links to newer posts in same cluster"
- "Strengthen the editorial argument — the original reads as topic summary"

STEP 8 — UPDATE RUN LOG
<ISO timestamp> | routine-6 | success | quarterly refresh sweep | posts_indexed:<n> | refresh_candidates:<n>

STEP 9 — COMMIT REPO
Stage references/url-database.md, references/category-tag-map.md, content-calendar.md, run-log.md.

Commit:
  Routine 6: quarterly URL database rebuild [posts:<n>] + refresh queue [candidates:<n>]
Push.

STEP 10 — EMAIL ROBERT
Send to ${ARAHKAII_AUTHOR_EMAIL}:
- Subject: "arahkaii Q<N> <YYYY> maintenance — refresh queue ready"
- Body:

# arahkaii Quarterly Maintenance — Q<N> <YYYY>

## URL Database Rebuilt
- Total published posts indexed: <n>
- Categories: <n>
- Tags: <n>
- Topic clusters identified: <n>
- Casing duplicates flagged: <n> (see references/category-tag-map.md)

## Refresh Queue
[Top 15 posts in priority order. For each: title, last modified, decay metric, suggested angle.]

## Recommended Actions
1. Review the 15 refresh candidates. Promote any to status:ready in content-calendar.md to trigger Routine 1 next time.
2. Reconcile casing duplicates flagged in category-tag-map.md (if any).
3. Consider archiving posts with <100 monthly visits and >365 days since modified (Robert decides; routine doesn't auto-archive).

SUCCESS CRITERIA
- url-database.md rebuilt with current data
- category-tag-map.md rebuilt
- content-calendar.md has refresh queue appended
- run-log.md updated
- All four files committed and pushed
- Email sent with summary

NEVER:
- Modify any WordPress content
- Delete any tags or categories
- Reorder or remove existing entries in content-calendar.md (only append)
```

---

## Why this routine matters

The URL database is the single most-referenced file by Routine 1 (drafting). When it's accurate, internal linking is precise. When it's stale, internal links go to outdated posts or miss recent additions.

The refresh queue addresses content decay — the slow, invisible loss of traffic on posts that were once strong. Without quarterly attention, decay compounds into significant traffic loss over a year.

---

## Tuning over time

After the first 4-6 quarterly runs:
- Compare which refresh angle suggestions actually resulted in traffic recovery (Routine 3's weekly digest tracks this)
- Tune the auto-suggest logic in step 7 to favor the angles that work for arahkaii specifically

---

*Maintained by Robert. The scoring formula in step 6 can be tuned based on what actually predicts refresh-worthy posts.*
